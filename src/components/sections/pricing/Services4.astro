---
interface Service {
  name: string;
  price: string;
}

interface Category {
  title: string;
  icon: string;
  services: Service[];
}

interface Props {
  title: string;
  subtitle: string;
  categories: Category[];
}

const { title, subtitle, categories } = Astro.props;
---

<section class="py-16 md:py-20 lg:py-24 bg-gray-50">
  <div class="max-w-[1344px] mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12 lg:mb-16">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        {title}
      </h2>
      <p class="text-base md:text-lg text-gray-600 max-w-3xl mx-auto">
        {subtitle}
      </p>
    </div>

    <!-- Categories Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
      {categories.map((category) => (
        <details class="bg-white rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow duration-300">
          <summary class="cursor-pointer list-none">
            <div class="flex items-center gap-3 mb-4">
              <i class={`${category.icon} text-2xl text-red-600`}></i>
              <h3 class="text-xl md:text-2xl font-semibold text-gray-900">
                {category.title}
              </h3>
            </div>
            <div class="w-full py-3 px-4 border border-gray-200 rounded-lg flex items-center justify-center gap-2 text-gray-700 font-medium text-sm transition-all duration-200 toggle-btn hover:border-red-600 hover:bg-red-50 hover:text-red-600">
              <span class="btn-show">Показать цены</span>
              <span class="btn-hide">Скрыть цены</span>
              <i class="ri-add-line text-lg icon-show"></i>
              <i class="ri-subtract-line text-lg icon-hide"></i>
            </div>
          </summary>

          <!-- Services List -->
          <div class="mt-6 space-y-4">
            {category.services.map((service) => (
              <div class="flex items-start justify-between gap-4 pb-4 border-b border-gray-100 last:border-0 last:pb-0">
                <span class="text-sm md:text-base text-gray-700 flex-1">
                  {service.name}
                </span>
                <span class="text-sm md:text-base font-semibold text-red-600 whitespace-nowrap">
                  {service.price}
                </span>
              </div>
            ))}
          </div>
        </details>
      ))}
    </div>
  </div>
</section>

<style>
  /* Custom styling for details/summary */
  details summary::-webkit-details-marker {
    display: none;
  }

  details[open] summary {
    margin-bottom: 0;
  }

  /* Toggle button visibility based on details state */
  /* When closed - show "Показать" button and + icon */
  details:not([open]) .btn-hide,
  details:not([open]) .icon-hide {
    display: none;
  }

  /* When open - show "Скрыть" button and - icon */
  details[open] .btn-show,
  details[open] .icon-show {
    display: none;
  }

  /* Smooth animation for accordion */
  details[open] > div {
    animation: slideDown 0.3s ease-out;
  }

  @keyframes slideDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
